{
  "name": "wikiscroll",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "check": "for f in public/*.js public/translations/*.js worker/*.js scripts/*.mjs; do node --check \"$f\" || exit 1; done && node --test test/*.test.js",
    "deploy": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "wrangler": "4.134.0"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "2.6.2"
  }
}
