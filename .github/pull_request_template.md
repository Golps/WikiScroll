## What and why

<!-- What does this change, and what problem does it solve? Link the issue if there is one. -->

## How it was tested

- [ ] `node --test test/*.test.js` passes
- [ ] Checked by hand on a phone-sized viewport and on desktop
- [ ] Checked reduced motion and a right-to-left language (if layout changed)

<!-- Mention anything you could not test, e.g. physical iOS devices. -->

## Checklist

- [ ] Removed styles, scripts, translations and tests for anything this change replaces
- [ ] Bumped `?v=` asset versions and the service worker `SHELL`/`CACHE` if `public/` files changed
- [ ] Added translations for new interface text
- [ ] No keys, account IDs or personal data in code, docs or screenshots
